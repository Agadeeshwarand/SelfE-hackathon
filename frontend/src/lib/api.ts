const API_URL=import.meta.env.VITE_API_URL??"http://localhost:4000";
function getToken(){return localStorage.getItem("token")}
export async function api<T>(path:string,options:{method?:string;body?:unknown}={}):Promise<T>{const headers:Record<string,string>={...(getToken()?{Authorization:`Bearer ${getToken()}`}:{})};if(options.body!==undefined)headers["Content-Type"]="application/json";const res=await fetch(`${API_URL}/api${path}`,{method:options.method??"GET",headers,body:options.body!==undefined?JSON.stringify(options.body):undefined});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error((data as any).error??`Request failed with status ${res.status}`);return data as T}
export async function downloadFile(path:string){const res=await fetch(`${API_URL}/api${path}`,{headers:{...(getToken()?{Authorization:`Bearer ${getToken()}`}:{})}});if(!res.ok){const data=await res.json().catch(()=>({}));throw new Error((data as any).error??"Export failed")}const blob=await res.blob();const disposition=res.headers.get("content-disposition")??"";const match=disposition.match(/filename="([^"]+)"/);const filename=match?.[1]??"export.bin";const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
export function setToken(t:string){localStorage.setItem("token",t)}export function clearToken(){localStorage.removeItem("token")}export function isLoggedIn(){return Boolean(getToken())}
export type Role="ADMIN"|"MENTOR"|"TEAM_LEADER"|"TEAM_MEMBER";export interface AuthUser{id:string;email:string;role:Role;isActive:boolean;createdAt:string;studentProfile:any;mentorProfile:any}
export function homeForRole(role:Role){if(role==="ADMIN")return "/admin";if(role==="MENTOR")return "/mentor";return "/app"}

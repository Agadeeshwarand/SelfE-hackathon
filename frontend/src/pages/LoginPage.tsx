import {FormEvent,useState} from "react";import {Link,Navigate,useNavigate} from "react-router-dom";import {ArrowRight,LockKeyhole,Mail,ShieldCheck} from "lucide-react";import {useAuth} from "../lib/auth";import {homeForRole} from "../lib/api";import {Button,Field,Input,ErrorState,Spinner} from "../components/ui";
export default function LoginPage(){const{user,loading,login}=useAuth();const nav=useNavigate();const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[error,setError]=useState<string|null>(null);const[submitting,setSubmitting]=useState(false);if(!loading&&user)return <Navigate to={homeForRole(user.role)} replace/>;async function submit(e:FormEvent){e.preventDefault();setError(null);setSubmitting(true);try{const u=await login(email,pw);nav(homeForRole(u.role))}catch(x){setError(x instanceof Error?x.message:"Unable to sign in")}finally{setSubmitting(false)}}return <div className="min-h-screen bg-slate-950"><div className="grid min-h-screen lg:grid-cols-[1fr_520px]"><div className="relative hidden overflow-hidden lg:block"><div className="absolute inset-0 bg-[radial-gradient(circle_at_25%_20%,rgba(99,102,241,.28),transparent_35%),radial-gradient(circle_at_75%_80%,rgba(14,165,233,.14),transparent_30%)]"/><div className="relative flex h-full flex-col justify-between p-12 xl:p-20"><Link to="/" className="flex items-center gap-3 text-white"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-white text-sm font-extrabold text-slate-950">H</span><span className="font-bold">SelfE Hackathon</span></Link><div className="max-w-xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo-300">Organizer + participant workspace</p><h1 className="mt-5 text-5xl font-extrabold tracking-[-.04em] text-white">Everything important, in one place.</h1><p className="mt-6 max-w-lg text-base leading-8 text-slate-400">Manage registration, teams, eligibility, mentors and operational exports from a single system.</p><div className="mt-8 flex flex-wrap gap-2">{["Teams","Mentors","Eligibility","Exports"].map(x=><span key={x} className="rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-xs font-semibold text-slate-300">{x}</span>)}</div></div><p className="text-xs text-slate-500">SelfE Hackathon 2026 · Secure role-based access</p></div></div><div className="flex items-center bg-white px-5 py-10 sm:px-10"><div className="mx-auto w-full max-w-md"><div className="mb-8"><Link to="/" className="flex items-center gap-2 text-sm font-bold text-slate-900 lg:hidden"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-slate-950 text-xs text-white">H</span>SelfE Hackathon</Link><p className="mt-8 text-xs font-bold uppercase tracking-[.15em] text-indigo-600">Welcome back</p><h2 className="mt-2 text-3xl font-extrabold tracking-tight text-slate-950">Sign in to your workspace</h2><p className="mt-2 text-sm text-slate-500">Use the account created by your registration or administrator.</p></div><form onSubmit={submit} className="space-y-5">{error&&<ErrorState message={error}/>}<Field label="Email"><div className="relative"><Mail className="absolute left-3.5 top-3.5 text-slate-400" size={17}/><Input className="pl-10" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div></Field><Field label="Password"><div className="relative"><LockKeyhole className="absolute left-3.5 top-3.5 text-slate-400" size={17}/><Input className="pl-10" type="password" required value={pw} onChange={e=>setPw(e.target.value)} placeholder="Enter your password"/></div></Field><Button size="lg" className="w-full" disabled={submitting}>{submitting?<><Spinner/>Signing in</>:<>Continue <ArrowRight size={16}/></>}</Button></form><div className="mt-7 flex items-center gap-2 text-xs text-slate-400"><ShieldCheck size={15} className="text-emerald-500"/>Your role controls what you can access.</div><p className="mt-7 text-center text-sm text-slate-500">New student? <Link to="/register" className="font-bold text-indigo-600 hover:text-indigo-700">Create an account</Link></p></div></div></div></div>}
